import {StyleSheet} from 'react-native'
const estilos = StyleSheet.create({
  fotoUsuario:{
    width:40,
    height:40,
    borderRadius:30,
    margin:10,
  },
  cabecalho:{
    flexDirection:"row",
    alignItems:"center",
  }
})

export default estilos;